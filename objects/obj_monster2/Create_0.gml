/// @description Insert description here
// You can write your code in this editor
// Create Event of obj_monster

// Movement speed
speed = 2;

// Define endpoints (starting point and turning point)
x_start = x;
y_start = y;
x_end = x + 100; // 100 pixels to the right of the starting point
y_end = y;

// Movement direction (1 for moving to x_end, -1 for moving to x_start)
move_dir = 1;
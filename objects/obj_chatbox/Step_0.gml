//if (active && keyboard_check_pressed(vk_enter)) { // 假设使用空格键翻页
    //current_page += 1; // 移动到下一页
   // if (current_page >= array_length(dialogue_pages)) {
        //active = false; // 如果已经是最后一页，关闭对话框
        //current_page = 0; // 重置页面索引，以便下次再次使用
   // }
//}